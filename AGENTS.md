# xtbflow：当前 AI agent 执行合同

## 1. 当前任务与权限

用户指定 `git@github.com:askusk521-cyber/xtbflow.git` 为远程仓库，并明确表示“ai拥有全部权限”。将本仓库作为新工作的版本化主仓库。已授权项目内读写、开发、提交、推送、问题维护、软件安装、数据处理及计算执行；不因历史模板的默认关闭值反复请求相同授权。

本文件与 `configs/` 是当前合同。`archive/` 是不可作为现行命令执行的历史材料；其中零预算、未授权和只允许写 starter 目录的旧描述不再阻塞本项目。没有实际凭据、队列资源或数据时，如实报告技术缺项；用户授权不能替代物理连接。

本次授权的对象是 xtbflow 项目，不是任意账户、其他仓库或无关文件。保留旧工程未提交修改，不默认强制推送、清除工作区、删除原始证据或改变仓库公开性。不要索取聊天中的私钥、密码或 Token。

## 2. 先做什么

1. 核对当前仓库与 origin，读取现行配置及来源记录。先区分真实现有文件、旧工程路径说明和未执行计划。
2. 在实际可访问的旧工程中，复用 UniTS、事件编译与数据审计资产；仅对变化或证据缺口做增量检查。旧工程的 dirty HEAD 不能当作新仓库冻结版本。
3. 生成 `public_data_manifest`，使用真实版本、记录 ID、母反应分组、构型哈希、标签来源和许可。没有真实数据时不填假种子或假成绩。
4. 完成 xTBloom 单位、力符号、有限差分、CPU/CUDA 对照、独立实现一致性及失败隔离测试。冻结实际版本与构建，不把历史 README 描述冒充安装能力。
5. 第一轮可在新增 DFT 为零的条件下，使用已有参考能量／力标签、同构型 GFN2 补算和小型标量 Delta 能量模型，开展分组留出及流匹配诊断。
6. 同时准备 CP2K 能量／力接口；新构型、新适用域及新通道需要新增参考验证时再使用。VibeQC 升级为正式参考后端前，重新核对所选方法的能量／力、边界和精度。
7. 通过阶段证据后自主进入下一阶段，无需仅为例行权限重新确认；每次扩大计算都先提交有明确硬上限的运行配置和决策记录。

## 3. 预算与环境

初始批次上限是 agent 在用户授权下采用的保守默认值，不是用户确认的设备容量或运行耗时。先探测现有配额与连接，取资源可用量与阶段上限中较小者。后续可依据实测成本自主修订并记录，不能使用无上限重试、无上限并发或无上限存储。

新付费服务、账单或资源采购不由一个仓库写入授权自动成立。现有计算资源可按项目授权使用；没有账单授权时不新建收费资源。

主机配置写入被忽略的 `configs/host.local.yaml` 或安全环境变量；公开记录只保留逻辑路径、方法身份、非敏感哈希与汇总成本。

## 4. 科学与数据边界

- 研究核心仍然是 flow matching；漂亮展示、发表目标和模块数量不能替代实证创新。
- 推理输入不得偷偷包含真实产物、参考 TS、参考模态、由目标 TS 反选的水或封存测试标签。训练侧合法标签与明确标记的端点条件诊断允许使用。
- 公开几何数据不能直接冒充独立反应物竞争数据。`public_data_manifest` 与 `independent_reactant_seed_manifest` 分开维护；后者必须在相应搜索前冻结。
- 不根据缺失元数据默认中性、单重态、溶剂或反应家族。完整原子／氢池和计数约定必须明确。
- Delta 能量和力标签来自同构型、同原子顺序、明确对应的状态与环境；不得将不同优化结果当作同构型配对。
- 普通能量下降不等于鞍点搜索；流时间不是物理时间；采样频率不是反应通量；事件连续变量不是电子密度。
- 分开记录未搜索、失败、替代通道、半经验验证、参考单点、参考 TS 连通等证据状态。半经验失败不等于化学事件不存在。
- 基线获得同样输入、校正势、优化器和计算预算。报告模型原始结果与统一后处理后的结果。
- 同一母反应及其构象、轨迹、增强、多保真标签不得跨组泄漏；预训练也纳入审计。
- 测试隔离与原始数据不可变是研究规则，不是对项目操作授权的撤销。原始文件通过派生版本变换；密钥、受限数据和封存标签不进 Git。

## 5. 人员与结论

用户是项目授权方，具体人类评审人尚未指定，不得杜撰身份。公开数据原型不等待实验合作者；新增湿实验暂缓。进入正式参考协议和机理结论前应安排有相应能力的审查，并记录审查是否真正独立。同一 agent 换提示词不构成独立验证。

计算研究可以提出有参考证据支持的解释和可检验预测；不得声称新增实验已完成、唯一机理已证实或保证高水平期刊录用。

## 6. 每轮交付

提交可审查的代码、实际 commit、锁定配置、真实清单与输入哈希、原始计算日志的可访问位置、机器可读指标、累计成本和失败归因。日志中的主机路径在公开前清理。展示脚本必须来自真实数据。

报告四件事：新增证据、真实成本、被支持或削弱的假设、下一项最有区分力的工作。仓库上传、环境安装、模型训练和物理验证是四种不同的完成状态。
