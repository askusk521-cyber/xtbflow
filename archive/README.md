# 历史启动材料：非现行执行指令

两个先前生成的启动包已按原始字节保存于各自目录，不重新核验其中的文献与动态软件能力。这些文件用于版本溯源；当前指令来自仓库根目录 `AGENTS.md` 与 `configs/`。

用户于 2026-09-25 授予 xtbflow 项目完整操作权限，替代历史材料中的“未授权”“全部预算为零”“仅可写 starter 目录”等操作限制。此前资源描述不证明本次执行环境已连接。

`user_resources.redacted.yaml` 是用户提供的资源配置的脱敏历史副本。真实主机路径、主机标识和写入根目录没有发布，原始文件哈希见 `docs/PROVENANCE.md`。

保留历史原文不意味着接受其中所有方案为已验证科学结论。研究边界依旧以真实证据为准。
