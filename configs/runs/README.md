# Run configurations

Place frozen, public run manifests and stage-specific configuration templates
here. Runtime state, raw outputs, credentials, private host paths and model
weights belong under the ignored local run/data directories. Every real run
must reference one shared stage ledger and record its code, environment,
calculator, protocol and input hashes before execution.
